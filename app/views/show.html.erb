<h1>Articles</h1>

<ul>
    <% @articles.each do |article.id| %>
      <li>
        <a href="/articles/<%= article.id %>">
          <%= article.title %>
        </a>
      </li>
    <% end %>
  </ul>
